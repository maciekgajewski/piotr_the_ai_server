# TensorFlow wheels

Local cache for TensorFlow wheels used by the wake-word training image.

The no-AVX TensorFlow wheel is intentionally not committed because it is a large
third-party binary. Put it here when rebuilding the wake-word image locally.
