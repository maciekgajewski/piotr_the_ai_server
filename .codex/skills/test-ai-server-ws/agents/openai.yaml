interface:
  display_name: "Test AI Server WS"
  short_description: "Websocket protocol and client checks"
  default_prompt: "Use $test-ai-server-ws to validate websocket protocol or client disconnect behavior."

policy:
  allow_implicit_invocation: true
